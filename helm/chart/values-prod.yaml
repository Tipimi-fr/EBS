imagePullSecrets:
  - name: regcred

route:
  enabled: true
  host: tipimi-ebs.lt-mut-prod-scw.les-tilleuls.solutions # TODO: confirm once the prod namespace/ArgoCD app exist

meilisearch:
  persistence:
    enabled: true
    storageClass: "sbs-default"
    size: "1Gi"

redis:
  persistence:
    enabled: true
    storageClass: "sbs-default"
    size: "1Gi"

php:
  fixtureJob:
    enabled: false
  fixtureCron:
    enabled: false
