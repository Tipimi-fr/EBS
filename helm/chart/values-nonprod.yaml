imagePullSecrets:
  - name: regcred

route:
  enabled: true
  host: tipimi-ebs.lt-mut-nonprod-scw.les-tilleuls.solutions

meilisearch:
  persistence:
    enabled: true
    storageClass: "sbs-default"
    size: "1Gi"

redis:
  persistence:
    enabled: true
    storageClass: "sbs-default"
    size: "1Gi"

php:
  image:
    repository: "rg.fr-par.scw.cloud/infra-mut-cr-scw/tipimi-ebs-php"
  fixtureJob:
    enabled: true
  fixtureCron:
    enabled: true

caddy:
  image:
    repository: "rg.fr-par.scw.cloud/infra-mut-cr-scw/tipimi-ebs-caddy"
